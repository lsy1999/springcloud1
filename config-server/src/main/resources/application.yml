server:
  port: 8888
spring:
  application:
    name :config-server
  cloud:
    config:
      server:
        git:
          #配置GitHub
          uri:
          search-paths:
          - configfiles
        #配置分支
        label: test